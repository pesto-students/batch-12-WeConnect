import React from 'react';
import FormControlLabel from '@material-ui/core/FormControlLabel';

const WeFormControll = (props) => {
  return <FormControlLabel {...props} />;
};

export default WeFormControll;
